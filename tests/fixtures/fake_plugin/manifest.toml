[plugin]
name = "fake"
version = "0.1.0"
entrypoint = "fake_plugin:FakePlugin"
blast_radius = "read-only"

[[capability]]
name = "ping"
description = "Returns pong."
input_schema = {}
output_schema = { reply = "string" }
