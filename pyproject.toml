[project]
name = 'butter-agent'
version = '0.0.1'
description = 'Local-first open-source conversational agent runtime. I pass butter.'
readme = 'README.md'
requires-python = '>=3.12'
license = { text = 'MIT' }
authors = [{ name = 'Shane Grech' }]
dependencies = []

[project.scripts]
butter = 'butter_agent.repl:main'

[dependency-groups]
dev = [
    'pytest>=8.0',
    'pytest-asyncio>=0.23',
    'ruff>=0.6',
    'mypy>=1.10',
]

[build-system]
requires = ['hatchling']
build-backend = 'hatchling.build'

[tool.hatch.build.targets.wheel]
packages = ['src/butter_agent']

[tool.ruff]
line-length = 320
target-version = 'py312'
src = ['src', 'tests']

[tool.ruff.format]
quote-style = 'single'
indent-style = 'space'

[tool.ruff.lint]
select = ['E', 'F', 'I', 'UP', 'B', 'SIM', 'RUF']
ignore = []

[tool.ruff.lint.isort]
known-first-party = ['butter_agent']

[tool.mypy]
python_version = '3.12'
strict = true
files = ['src', 'tests']
warn_unused_ignores = true
warn_redundant_casts = true

[tool.pytest.ini_options]
testpaths = ['tests']
addopts = '-ra --strict-markers'
asyncio_mode = 'auto'
